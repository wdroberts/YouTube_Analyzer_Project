# YouTube Analyzer Project - Environment Configuration Template
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file and rename it to: .env
# 2. Fill in your actual values below
# 3. Never commit the .env file (it's protected by .gitignore)

# ============================================================================
# REQUIRED: OpenAI API Configuration
# ============================================================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================================================
# OPTIONAL: Application Configuration
# ============================================================================

# Audio Quality (kbps) - Valid range: 32-320
# Lower values = smaller files, faster processing
# Higher values = better audio quality
# Default: 96 (recommended for speech)
# Note: Files >24MB are automatically split into chunks regardless of quality
AUDIO_QUALITY=96

# OpenAI Model for text generation
# Options: gpt-4o-mini (faster, cheaper), gpt-4, gpt-3.5-turbo
# Default: gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# ============================================================================
# OPTIONAL: Advanced Settings (Uncomment to use)
# ============================================================================

# Maximum audio file size in MB before automatic chunking (Whisper API limit is 25MB)
# Files larger than this will be automatically split into chunks
# MAX_AUDIO_FILE_SIZE_MB=24

# Audio chunking settings (for files >24MB)
# AUDIO_CHUNK_SIZE_MB=20           # Target size for each chunk
# AUDIO_CHUNK_OVERLAP_MS=500       # Overlap to prevent mid-word cuts

# Rate limit between processing requests (seconds)
# RATE_LIMIT_SECONDS=5

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# ============================================================================
# Quick Start
# ============================================================================
# Windows: copy env.template .env
# Mac/Linux: cp env.template .env
# 
# Then edit .env with your favorite text editor and add your OPENAI_API_KEY
# Run the app: streamlit run app.py.py

